.container
  .row
    .col-sm-12

      %h3 Message Boards

      .message-board
      
        %h2 Your subscriptions
        = paginate @topics

        - @topics.each do |sub|
          = render partial: 'topic', :locals => {:topic => sub.subscribable}
